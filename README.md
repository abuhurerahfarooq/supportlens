# SupportLens

Lightweight observability platform for support chatbot.

## Setup

1. Create `.env`
OPENAI_API_KEY=your_key_here

2. Run:
docker-compose up --build

3. Open:
http://localhost:5000

## Endpoints

POST /traces
GET /traces
GET /analytics
GET /analytics/trends